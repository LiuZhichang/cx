#pragma once

#include "cx/graphics/pipeline/pipeline.h"

namespace cx::graphics {

class GraphicsPipeline : public Pipeline {
 public:
  enum class Mode { ePolygon, eMrt };

  // GraphicsPipeline(Stage, std::vector<std::filesystem::path> shader_stages,
  // std::vector<s>)
 private:
};

}  // namespace cx::graphics